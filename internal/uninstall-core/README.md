# internal/uninstall-core

Application discovery and leftover matching for the `uninstall` command: finding an app's
associated files by bundle identifier and name evidence, not by broad pattern guessing.

Not yet implemented. Phase 3 work, after the deletion engine and the `clean` command.
