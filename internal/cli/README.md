# internal/cli

Command definitions and terminal output rendering for non-interactive use: scripting,
automation, and structured output such as `--json`. Distinct from `internal/terminal-shell`,
which owns the full-screen interactive experience.

Not yet implemented.
