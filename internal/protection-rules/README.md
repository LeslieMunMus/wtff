# internal/protection-rules

Declarative rules describing what must never be deleted, loaded from data rather than hardcoded
as logic. Every rule carries a stated reason and a source (an Apple document, a vendor document,
or direct system inspection), not just a pattern to match against.

Not yet implemented. The rule schema is Opus-tier work; authoring individual rule entries once
the schema exists is Sonnet-tier work.
