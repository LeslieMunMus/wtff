# internal/terminal-shell

The full-screen interactive terminal interface, in the style of Claude Code's own CLI. See
`docs/architecture/terminal-interface.md` for the full requirement and design input.

Not yet implemented. Later phase, but the requirement is locked in from the start of the
project so earlier packages are built with a rendering boundary in mind.
